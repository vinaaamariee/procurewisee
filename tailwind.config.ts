import type { Config } from "tailwindcss";

// Tailwind v4: configuration is primarily done via @theme in globals.css.
// This file is kept only for compatibility with tooling that reads it.
const config: Config = {
  darkMode: "class",
  content: [],
  theme: {
    extend: {},
  },
};

export default config;
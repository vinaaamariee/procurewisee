"use client";

export default function HistoricalPriceChart() {
  return (
    <div
      className="flex items-center justify-center py-8 text-sm"
      style={{ color: "var(--text-muted)" }}
    >
      Price history is tracked through the Pre-Canvass process
    </div>
  );
}
